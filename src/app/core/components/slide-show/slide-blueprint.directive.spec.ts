/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SlideBlueprintDirective } from './slide-blueprint.directive';

describe('Directive: SlideBlueprint', () => {
  it('should create an instance', () => {
    const directive = new SlideBlueprintDirective();
    expect(directive).toBeTruthy();
  });
});
